name: SC FAIR-MAST P058 SELECT N100 ARCHIVED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'P058 selection is sealed; workflow disabled.'
