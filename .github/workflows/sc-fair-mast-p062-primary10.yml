name: SC FAIR-MAST P062 PRIMARY10 [ARCHIVED]
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'P062 primary acquisition is archived; authoritative run 32046373782.'
