name: SC FAIR-MAST P058 N100 STATE-ENRICHED ARCHIVED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Cancelled non-authority P058 state-enriched workflow; disabled.'
