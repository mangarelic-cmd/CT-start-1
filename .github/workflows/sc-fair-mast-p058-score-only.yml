name: SC FAIR-MAST P058 N100 SCORE ONLY ARCHIVED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'P058 N100 authority is sealed; reread disabled.'
