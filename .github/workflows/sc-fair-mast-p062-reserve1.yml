name: SC FAIR-MAST P062 RESERVE1 [ARCHIVED]
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'P062 reserve acquisition is archived; authoritative run 32046756745.'
